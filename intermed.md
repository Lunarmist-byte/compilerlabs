Start.
Read the expression.
Convert the expression into postfix form using operator precedence.
Scan the postfix expression.
For each operator, take the previous two operands and generate a temporary variable.
Replace the operation with the temporary variable.
Print the generated intermediate code.
Stop.
